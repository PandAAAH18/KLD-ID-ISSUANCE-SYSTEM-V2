<?php

// platform_check.php @generated by Composer

$issues = array();

if (PHP_VERSION_ID < 80000) {
    $issues[] = 'Your PHP version (' . PHP_VERSION . ') does not satisfy the required PHP version constraint (>= 8.0.0).';
}

if ($issues) {
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    $message = 'Composer detected issues in your platform:' . "\n\n";
    foreach ($issues as $issue) {
        $message .= '  - ' . $issue . "\n";
    }
    $message .= "\nTo resolve this issue, please update your PHP version or update your dependencies.\n";
    throw new RuntimeException($message);
}
